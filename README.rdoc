== README

Standalone web app built in Ruby on Rails. Uses the mailchimp-api gem, and when a user signups, the email is sent to your mailchimp account but it is also stored locally. Uses Google recaptcha to avoid spam.

===Installation:
* Git clone the repository

* Run the bundle command

* Migrate the database
